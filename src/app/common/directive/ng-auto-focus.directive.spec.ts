import { NgAutoFocusDirective } from './ng-auto-focus.directive';

describe('NgAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new NgAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
